{{-- Favicon --}}
<link rel="shortcut icon" href="{{ asset(settings('ico')) }}" />
<link rel="icon" href="{{ asset(settings('ico')) }}" sizes="32x32">
<link rel="icon" href="{{ asset(settings('ico')) }}" sizes="192x192">

<meta name="msapplication-TileImage" content="{{ asset(settings('ico')) }}">
<link rel="apple-touch-icon" href="{{ asset(settings('ico')) }}">
